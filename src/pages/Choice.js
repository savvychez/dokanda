import React, { useEffect, useState } from 'react';
import BigButton from '../components/BigButton';
import styled from "styled-components";


const Choice = () => {
  return (
    <div className="choices">
      <BigButton text="Doctor" icon=""/>
    </div>
  )
}

export default Choice;